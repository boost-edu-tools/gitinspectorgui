# Auto-generated by scripts/switch-ci.sh. DO NOT EDIT.
# Base GitLab CI/CD configuration
# Contains common stages and variables

stages:
    - test
    - build
    - docs
    - release

variables:
    CARGO_HOME: $CI_PROJECT_DIR/.cargo
    CARGO_TARGET_DIR: $CI_PROJECT_DIR/target

.cache_template: &cache_template
    cache:
        key: ${CI_COMMIT_REF_SLUG}
        paths:
            - .cargo/
            - target/
            - node_modules/
            - python/.venv/

# The Docker image that will be used to build your app
image: node:lts

# Placeholder job to satisfy GitLab CI requirements
# Individual pages disabled - documentation served via group pages
documentation_info:
    stage: test
    script:
        - echo "GitInspectorGUI Documentation Status:"
        - echo "- Individual project pages: DISABLED"
        - echo "- Documentation location: https://edu-boost.gitlab.io/gitinspectorgui/"
        - echo "- Deployment method: Group pages (centralized)"
        - echo "- Status: Active and accessible"

# Disabled individual project pages - documentation now served via group pages
# at https://edu-boost.gitlab.io/gitinspectorgui/
# 
# pages:
#     stage: docs
#     # Functions that should be executed before the build script is run
#     before_script:
#         - corepack enable
#         - pnpm install --frozen-lockfile
#     script:
#         - ./scripts/test-docs-build.sh
#         # Create the group pages structure
#         - mkdir -p public
#         - mv public_temp/gitinspectorgui public/gitinspectorgui
#     artifacts:
#         paths:
#             - public
#     rules:
#         # This ensures that only pushes to the default branch will trigger
#         # a pages deploy
#         - if: $CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH
